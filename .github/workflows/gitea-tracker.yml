name: Gitea Version Tracker

on:
  schedule:
    # Daily at 08:00 UTC — check for new Gitea releases
    - cron: '0 8 * * *'
  workflow_dispatch:
    inputs:
      version:
        description: 'Gitea version (e.g. 1.27.0) — leave empty to auto-detect'
        required: false

jobs:
  track:
    name: Track Gitea Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Determine latest Gitea version
        id: version
        run: |
          if [ -n "${{ github.event.inputs.version }}" ]; then
            NEW_VERSION="${{ github.event.inputs.version }}"
          else
            # Fetch latest release from Gitea API
            RELEASE_JSON=$(curl -sSf https://gitea.com/api/v1/repos/gitea/gitea/releases/latest)
            NEW_VERSION=$(echo "$RELEASE_JSON" | jq -r '.tag_name')
          fi
          # Strip 'v' prefix if present (API returns v1.27.0, we use 1.27.0)
          NEW_VERSION="${NEW_VERSION#v}"
          echo "New Gitea version: $NEW_VERSION"

          # Read current max-tested from the TRACKER:QUICK-REF-MAX marker line above it
          MARKER_LINE=$(grep -n 'TRACKER:QUICK-REF-MAX' COMPATIBILITY.md | cut -d: -f1)
          CURRENT=$(sed -n "$((MARKER_LINE - 1))p" COMPATIBILITY.md | grep -oP '\*\*[0-9.]+\*\*' | tail -1 | tr -d '*')
          echo "Current max tested: $CURRENT"

          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "current_version=$CURRENT" >> $GITHUB_OUTPUT

          if [ "$NEW_VERSION" = "$CURRENT" ]; then
            echo "No new Gitea release detected. Skipping."
            echo "skip=true" >> $GITHUB_OUTPUT
          else
            echo "New version detected: $CURRENT → $NEW_VERSION"
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: COMPATIBILITY.md — Quick Reference (max tested version)
        if: steps.version.outputs.skip != 'true'
        run: |
          NEW="${{ steps.version.outputs.new_version }}"
          # The Quick Reference row is the line directly above TRACKER:QUICK-REF-MAX
          MARKER=$(grep -n 'TRACKER:QUICK-REF-MAX' COMPATIBILITY.md | cut -d: -f1)
          TARGET=$((MARKER - 1))
          # Replace max-tested version (last **X.Y.Z**) + status in Quick Reference row
          sed -i "${TARGET}s/\*\*[0-9.]*\*\* | [^|]*/**${NEW}** | ⏳ Pending Verification/" COMPATIBILITY.md

      - name: COMPATIBILITY.md — Version History table
        if: steps.version.outputs.skip != 'true'
        run: |
          NEW="${{ steps.version.outputs.new_version }}"
          TODAY=$(date +%Y-%m-%d)
          # Insert new row after TRACKER:VERSION-INSERT marker (sits after table separator)
          MARKER=$(grep -n 'TRACKER:VERSION-INSERT' COMPATIBILITY.md | cut -d: -f1)
          ROW="| **${NEW}** | ${TODAY} | ⏳ Pending Verification | TBD |"
          sed -i "${MARKER}a ${ROW}" COMPATIBILITY.md

      - name: COMPATIBILITY.md — latest verified note
        if: steps.version.outputs.skip != 'true'
        run: |
          NEW="${{ steps.version.outputs.new_version }}"
          # Update version inline on the line with TRACKER:LATEST-VERIFIED
          sed -i "/TRACKER:LATEST-VERIFIED/s/Gitea [0-9.]*/Gitea ${NEW}/" COMPATIBILITY.md

      - name: README.md — badge and latest tested
        if: steps.version.outputs.skip != 'true'
        run: |
          NEW="${{ steps.version.outputs.new_version }}"
          # Badge: replace version between %20...%20tested-blue
          sed -i "/TRACKER:BADGE/s/%20[0-9a-z.-]*%20tested-blue/%20${NEW}%20tested-blue/" README.md
          # Latest tested: update version on the TRACKER:LATEST-TESTED line
          sed -i "/TRACKER:LATEST-TESTED/s/Gitea [0-9.]*/Gitea ${NEW}/" README.md


      - name: README translations — latest tested
        if: steps.version.outputs.skip != 'true'
        run: |
          NEW="${{ steps.version.outputs.new_version }}"
          for f in docs/README.{ja,ko,ru,zh-CN,zh-TW}.md; do
            sed -i "/TRACKER:LATEST-TESTED/s/Gitea [0-9.]*/Gitea ${NEW}/" "$f"
          done

      - name: Create Pull Request
        if: steps.version.outputs.skip != 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          branch: track/gitea-${{ steps.version.outputs.new_version }}
          commit-message: "docs: track Gitea ${{ steps.version.outputs.new_version }} — pending verification"
          title: "⬆️ Track Gitea ${{ steps.version.outputs.new_version }} — ⏳ Pending Verification"
          body: |
            Gitea **${{ steps.version.outputs.new_version }}** has been released.

            This PR:
            - Updates the compatibility matrix in `COMPATIBILITY.md`
            - Updates version badges in `README.md` and translations
            - Marks the new version as **⏳ Pending Verification**

            ## Verification Checklist
            - [ ] Check [Gitea changelog](https://github.com/go-gitea/gitea/blob/main/CHANGELOG.md) for mail template changes
            - [ ] Review diff of official templates: `git diff v${{ steps.version.outputs.current_version }}..v${{ steps.version.outputs.new_version }} -- templates/mail/ services/mailer/`
            - [ ] Run `cd tools && go run . preview all` to confirm templates still render
            - [ ] Update status from ⏳ to ✅ (compatible) or ❌ (breaking) in COMPATIBILITY.md
            - [ ] Update the `Mail Template Changes` and `Breaking?` columns with accurate notes
          labels: compatibility, automated
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
